declare module 't-writer.js' {
    // class Typewriter {
    // constructor(element: HTMLElement, options?: any);
    // type: (text: string) => this;
    // remove: (numChars: number) => this;
    // clear: () => this;
    // start: () => Promise<void>;
    // }
    export default Typewriter;
}
